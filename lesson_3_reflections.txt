When would you want to create a remote repository rather than keeping your work local?

In order to share code.

Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?

To reduce the chances of error.

Describe the differences between forks, clones, and branches. When would you use one instead of another?

Forks - It is used for cloning a remote file on your computer. To clone a file on Github on local directory.

Clones- create clones on your local machine.

Branches- within your repository.

What is the benefit of having a copy of the last known state of the remote stored locally?
It is easier to track and make changes.